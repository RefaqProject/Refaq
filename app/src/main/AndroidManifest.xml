<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elashry.refaq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Family_2"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AuthActivity"
            android:label="@string/title_activity_auth"
            android:parentActivityName=".IntroActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".IntroActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_profile"
            android:parentActivityName=".IntroActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".IntroActivity" />
        </activity>
        <activity android:name=".RegisterKafeel" />
        <activity android:name=".RegisterFakeer" />
        <activity android:name=".RegisterFamily" />
        <activity android:name=".Registermtabr3" />
        <activity android:name=".FamilyDetails" />
        <activity
            android:name=".News"
            android:theme="@style/AppTheme" />
        <activity android:name=".About" />
        <activity android:name=".Programs" />
        <activity
            android:name=".CallUs"
            android:theme="@style/AppTheme" />
        <activity android:name=".Family" />
        <activity android:name=".IntroActivity" />

    </application>

</manifest>